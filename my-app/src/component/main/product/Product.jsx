import React from 'react';
import Section from '../../Section';

const Product = () => {
    return (
        <main>
            <Section
                title="Productos"
                route='/products'
            />
        </main>
    );
}

export default Product;
